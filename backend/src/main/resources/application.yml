server:
  port: 8080

logging:
  level:
    root: INFO
    com.orvix: DEBUG

orvix:
  projects-root: ${ORVIX_PROJECTS_ROOT:C:/Users/venka/.gemini/antigravity/scratch/orvix/projects}
  mock-mode:
    enabled: ${ORVIX_MOCK_MODE_ENABLED:false}
